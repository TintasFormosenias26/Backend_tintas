services:
  api:
    build: .
    container_name: backend
    ports:
      - "3400:3400"
    env_file:
      - .env
    volumes:
      - .:/app
    command: sh -c "pnpm prisma generate && npm run dev"

volumes:
  pgdata:
